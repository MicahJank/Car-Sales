
const initialState = 0;

const additionalPrice = (state = initialState, action) => {
    switch(action.type) {
        // cases here
        default:
            return state;
    }
}

export default additionalPrice;